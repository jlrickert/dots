# dots
A brew-style dotfile package manager with taps, profiles, overlays, and cross-platform support. dots treats Git repos as package sources, uses path aliases for portability, and supports symlink/copy/hardlink link strategies with platform-aware resolution.
